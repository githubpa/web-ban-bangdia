@model WEBBANGDIA.Models.LoaiTaiKhoan

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>LoaiTaiKhoan</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.MaLoaiTK)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MaLoaiTK)
            @Html.ValidationMessageFor(model => model.MaLoaiTK)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.LoaiTK)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.LoaiTK)
            @Html.ValidationMessageFor(model => model.LoaiTK)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
