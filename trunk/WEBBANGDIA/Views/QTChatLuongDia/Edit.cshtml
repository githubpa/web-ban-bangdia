@model WEBBANGDIA.Models.ChatLuong

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ChatLuong</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.MaCL)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MaCL)
            @Html.ValidationMessageFor(model => model.MaCL)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.TenCL)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TenCL)
            @Html.ValidationMessageFor(model => model.TenCL)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
