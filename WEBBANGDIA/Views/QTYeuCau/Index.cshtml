@model IEnumerable<WEBBANGDIA.Models.YeuCau>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.MaYC)*@Mã yêu cầu
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.TenYeuCau)*@Tên yêu cầu
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaYC)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenYeuCau)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.MaYC }) |
            @Html.ActionLink("Details", "Details", new { id = item.MaYC }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.MaYC })
        </td>
    </tr>
}

</table>
